
# =======================================================
# SOLUSI FINAL FIX ERROR 403 & MIME TYPE (VM GCP)
# =======================================================

# 1. PERBAIKI IZIN AKSES FOLDER (WAJIB JALANKAN INI)
# Nginx butuh izin "X" (execute) pada setiap level folder agar bisa masuk.
sudo chmod 755 /home/clusterkobapwk
sudo chmod 755 /home/clusterkobapwk/warung-app
sudo chmod 755 /home/clusterkobapwk/warung-app/sim-rw
sudo chmod 644 /home/clusterkobapwk/warung-app/sim-rw/index.html
sudo chmod 644 /home/clusterkobapwk/warung-app/sim-rw/index.tsx

# 2. EDIT KONFIGURASI NGINX
# sudo nano /etc/nginx/sites-available/default

# COPY-PASTE KODE DI BAWAH INI (Hapus semua isi lama):

server {
    listen 80;
    server_name _;

    root /home/clusterkobapwk/warung-app/sim-rw;
    index index.html;

    # FIX MIME TYPE UNTUK .tsx
    include /etc/nginx/mime.types;
    types {
        application/javascript tsx;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    # REVERSE PROXY KE BACKEND PORT 5000
    location /api/ {
        proxy_pass http://localhost:5000/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# 3. RESTART NGINX
# sudo nginx -t
# sudo systemctl restart nginx
